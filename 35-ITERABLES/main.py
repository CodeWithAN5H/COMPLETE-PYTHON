# Iterables = An object/collection that can return its elements one at a time,
#             allowing it to be iterated over in a loop.

my_dict = {
    'a': 1,
    'b': 2,
    'c': 3,
}
print(my_dict)
for key in my_dict:
    print(key)

# USE LECTURES FOR EXPLANATION